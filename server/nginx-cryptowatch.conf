server {
    listen 80;
    server_name cryptowatch.sifuture.cn;

    # 日志
    access_log /var/log/nginx/cryptowatch-access.log;
    error_log /var/log/nginx/cryptowatch-error.log;

    # API代理
    location /api/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        
        # SSE支持（扣子AI流式响应）
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 300s;
    }

    # 静态文件（如果需要）
    location / {
        root /var/www/CryptoWatchDemo/public;
        try_files $uri $uri/ /index.html;
        
        # CORS
        add_header 'Access-Control-Allow-Origin' '*' always;
        add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS' always;
        add_header 'Access-Control-Allow-Headers' 'Content-Type' always;
    }
}

# HTTPS配置（部署SSL证书后取消注释）
# server {
#     listen 443 ssl http2;
#     server_name cryptowatch.sifuture.cn;
# 
#     ssl_certificate /etc/nginx/ssl/cryptowatch.crt;
#     ssl_certificate_key /etc/nginx/ssl/cryptowatch.key;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
# 
#     access_log /var/log/nginx/cryptowatch-ssl-access.log;
#     error_log /var/log/nginx/cryptowatch-ssl-error.log;
# 
#     location /api/ {
#         proxy_pass http://127.0.0.1:8080;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_cache_bypass $http_upgrade;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         
#         proxy_buffering off;
#         proxy_cache off;
#         proxy_read_timeout 300s;
#     }
# 
#     location / {
#         root /var/www/CryptoWatchDemo/public;
#         try_files $uri $uri/ /index.html;
#     }
# }
# 
# # HTTP重定向到HTTPS
# server {
#     listen 80;
#     server_name cryptowatch.sifuture.cn;
#     return 301 https://$server_name$request_uri;
# }
