# CryptoWatch 智能对话项目 - 操作指南

> **最后更新：** 2026年1月14日 21:15  
> **文档性质：** 当前进度和操作指南（紧密结合项目设计.md）

---

## 🎯 当前进度总结（2026-01-14）

### ✅ 已完成的工作

1. **Python环境准备** ✅
   - Python 3.12.1已安装成功
   - 位置：系统PATH中可用
   - 验证：`python --version` 输出正常

2. **本地服务器部署** ⚠️ **部分工作**
   - server.js在8080端口启动（进程ID: 17588）
   - HTML测试页面可访问：http://localhost:8080/voice_chat_complete.html
   - 用户已尝试访问页面（看到GET请求日志）
   - 用户已尝试录音功能（看到POST /api/aliyun/asr请求）

3. **配置文件创建** ✅
   - 已创建`server-deploy/aliyun_config.json`
   - 使用项目设计.md中提供的appKey和token
   - ⚠️ Token可能今天（1月14日）失效

### ⚠️ 当前问题

**核心问题：server.js调用Python失败**
- **原因：** server.js使用`python3`命令，但Windows上应该是`python`
- **影响：** 无法动态生成阿里云Token，配置接口返回错误
- **日志：** `[错误] Python失败: Python was not found`
- **需要修复：** 将server.js第56行的`python3`改为`python`

**次要问题：Token可能失效**
- 项目设计.md说明：token在1月14日失效（今天）
- 如果失效，需要用Python脚本重新生成

### � 关键发现（为什么之前能运行现在不行）

**历史问题分析：**
1. **之前可能在Linux/Mac环境测试** - 那里用`python3`命令
2. **或者之前有缓存的Token** - aliyun_config.json文件存在且未过期
3. **现在是纯Windows环境** - 命令应该是`python`而非`python3`

---

## 📂 项目文件结构与作用说明

### 🗂️ 核心目录

```
watch/
├── CryptoPhone/          ⭐ 主力项目（手机端uni-app）
├── CryptoWatch/          📦 备份版本（原始代码）
├── server-deploy/        🖥️ 服务器端代码
├── web-test-pages/       🧪 HTML测试页面（重要参考）
├── 归档文档/             📚 历史文档归档
└── [根目录文件]          🛠️ 工具脚本和文档
```

### 📄 根目录关键文件

| 文件名 | 作用 | 重要性 |
|--------|------|--------|
| **项目设计.md** | 需求、目标、API密钥、改进须知 | ⭐⭐⭐ 必读 |
| **操作.md** | 当前进度、操作步骤、问题记录 | ⭐⭐⭐ 当前文档 |
| **文件夹说明.md** | 完整项目结构说明 | ⭐⭐ 参考 |
| **服务器配置代码汇总与解释.md** | 服务器配置说明 | ⭐⭐ 服务器相关 |
| get_aliyun_token.py | 动态生成阿里云Token的Python脚本 | ⭐⭐⭐ 关键工具 |
| install_python.ps1 | 自动安装Python 3.12的脚本 | ⭐ 已执行 |
| test_server_api.ps1 | 测试服务器API的脚本 | ⭐ 调试工具 |
| 清理无用文件.ps1 | 清理项目文件的脚本 | ⭐ 维护工具 |

### 📁 server-deploy/ 目录（服务器端）

| 文件名 | 作用 | 状态 |
|--------|------|------|
| **server.js** | Node.js主服务文件，提供所有API代理 | ⚠️ 需修复python3→python |
| server-fixed.js | 备份版本（可能是之前的稳定版） | 📦 备用 |
| package.json | Node.js依赖配置 | ✅ 正常 |
| get_aliyun_token.py | 阿里云Token生成脚本（复制） | ✅ 正常 |
| aliyun_config.json | 阿里云配置缓存文件 | ⚠️ Token可能过期 |
| nginx-cryptowatch.conf | Nginx配置（服务器用） | 📦 服务器部署用 |
| cryptowatch.service | Systemd服务配置（Linux） | 📦 服务器部署用 |
| deploy.sh | 部署脚本（Linux） | 📦 服务器部署用 |
| 快速部署.ps1 | Windows部署脚本 | 📦 部署工具 |

### 📁 web-test-pages/ 目录（HTML测试页面）

| 文件名 | 作用 | 重要性 |
|--------|------|--------|
| **voice_chat_complete.html** | 完整语音对话测试页面 | ⭐⭐⭐ 最重要参考 |
| test_asr_only.html | 仅ASR识别测试 | ⭐ 单独测试 |
| test_tts_only.html | 仅TTS合成测试 | ⭐ 单独测试 |
| test_asr_restful.html | ASR RESTful测试 | ⭐ 单独测试 |

**voice_chat_complete.html 的价值：**
- ✅ 已验证可工作的完整实现
- ✅ 包含正确的SSE响应解析（关键！）
- ✅ 录音格式转换（WebM→WAV）
- ✅ 完整的错误处理
- 📌 修复CryptoPhone时的重要参考

### 📁 CryptoPhone/ 目录（手机uni-app）

```
CryptoPhone/
├── pages/index/index.vue    # 主页面（语音对话+加密货币展示）
├── api/                      # API封装
│   ├── voice.js             # 阿里云语音API（ASR/TTS）
│   ├── ai.js                # 扣子AI对话API
│   └── crypto.js            # 加密货币数据API
├── utils/
│   ├── config.js            # 服务器地址配置
│   └── request.js           # HTTP请求封装
├── components/              # 组件
│   ├── CryptoCarousel.vue   # 加密货币轮播
│   └── MessageBubble.vue    # 消息气泡
├── keystore/                # Android签名证书
│   └── cryptowatch.keystore # 证书文件
├── manifest.json            # uni-app配置
├── pages.json               # 页面配置
└── README.md                # 项目说明
```

**已知问题（需要修复）：**
- ⚠️ AI响应解析可能不正确（需参考HTML的SSE解析）
- ⚠️ 录音权限申请时机可能有问题
- ⚠️ 功能未经过实际测试验证

---

## 🔧 关键技术细节（从HTML测试页面学到的）

### 1. 阿里云配置加载
```javascript
// 从服务器获取配置
const response = await fetch('/api/aliyun/config');
const config = await response.json();
// 注意：服务器返回的是 appKey（大写K）
appKey = config.appKey || config.appkey;
token = config.token;
```

### 2. 录音设置（重要参数）
```javascript
navigator.mediaDevices.getUserMedia({ 
    audio: {
        sampleRate: 16000,      // 16kHz
        channelCount: 1,         // 单声道
        echoCancellation: true,
        noiseSuppression: true
    } 
})
```

### 3. ASR识别流程
```javascript
// 1. WebM → WAV转换（16kHz, 16-bit, 单声道）
const wavBlob = await convertToWav(audioBlob);

// 2. 通过URL参数传递token和appkey
const url = `/api/aliyun/asr?appkey=${appKey}&token=${token}`;

// 3. body直接传音频二进制数据
await fetch(url, { method: 'POST', body: wavBlob });
```

### 4. AI对话 - SSE响应解析 ⭐⭐⭐ 最关键
```javascript
// 扣子AI返回Server-Sent Events格式，必须特殊处理
const text = await response.text();
const lines = text.split('\n');
let fullAnswer = '';

for (const line of lines) {
    if (line.startsWith('data: ')) {
        const jsonStr = line.substring(6); // 去掉 "data: " 前缀
        const data = JSON.parse(jsonStr);
        
        // 只提取 type === 'answer' 的内容
        if (data.type === 'answer' && data.content?.answer) {
            fullAnswer += data.content.answer;
        }
    }
}
```

### 5. TTS播放
```javascript
// 所有参数通过URL传递
const url = `/api/aliyun/tts?` + new URLSearchParams({
    appkey: appKey,
    token: token,
    text: text,
    voice: 'aixia',
    format: 'mp3',
    sample_rate: 16000
});

const audio = new Audio(url);
await audio.play();
```

---

## 🔑 核心配置信息（快速查阅）

### 阿里云语音交互
- **凭证信息已移除**，请使用环境变量或 `aliyun_config.json`
- **Token生成:** 运行`python get_aliyun_token.py --json`

### 扣子AI（中本聪智能体）
- **Project ID:** `7594865794518106153`
- **API URL:** `https://m3t2y79rsr.coze.site/stream_run`
- **Token:** 见项目设计.md（很长）

### 服务器
- **本地测试:** http://localhost:8080
- **远程服务器:** 47.97.121.208:8080
- **SSH:** diana@47.97.121.208（密码：a）

### Android证书
- **路径:** `CryptoPhone/keystore/cryptowatch.keystore`
- **密码:** `cryptowatch2026`
- **别名:** `cryptowatch`

> 💡 完整密钥和配置详见 [项目设计.md](项目设计.md)

---

## 📋 下一步操作计划（重启对话后执行）
   - 生成自有Android证书：`CryptoPhone/keystore/cryptowatch.keystore`
   - 删除AI生成的旧证书：`D:\test.keystore`

3. **代码改进**
   - 权限申请优化（uni.authorize）
   - 错误处理增强
   - Mock数据真实化（BTC: ~$94,960）

4. **文件夹整理**
   - 删除空文件夹：`test/`、`try/`
   - 归档冗余文档到`归档文档/`
   - 更新操作.md为精简版
   - 新建`文件夹说明.md`结构指南

### 🔍 HTML测试页面分析（重要参考）

**voice_chat_complete.html 关键实现细节：**

1. **配置加载** (`/api/aliyun/config`)
   - 从服务器获取appKey和token
   - 注意：服务器返回的是`appKey`（大写K）

2. **录音设置**
   ```javascript
   audio: {
       sampleRate: 16000,
       channelCount: 1,
       echoCancellation: true,
       noiseSuppression: true
   }
   ```

3. **ASR识别** (`/api/aliyun/asr`)
   - 先将webm转换为WAV格式（16kHz, 16-bit, 单声道）
   - 使用URL参数传递appkey和token
   - body直接传音频二进制数据

4. **AI对话** (`/api/coze`) ⭐ 关键点
   - 使用Server-Sent Events (SSE)格式返回
   - 需要解析`data: `开头的行
   - 提取`type === 'answer'`的内容并拼接
   ```javascript
   const lines = text.split('\n');
   let fullAnswer = '';
   for (const line of lines) {
       if (line.startsWith('data: ')) {
           const data = JSON.parse(line.substring(6));
           if (data.type === 'answer' && data.content?.answer) {
               fullAnswer += data.content.answer;
           }
       }
   }
   ```

5. **TTS播放** (`/api/aliyun/tts`)
   - 使用URL参数传递所有配置
   - 直接创建Audio对象播放URL

### 📋 下一步操作计划（按优先级）

**🎯 当前任务：测试HTML完整流程**（进行中）
1. ✅ 本地启动server.js
2. ✅ 打开HTML测试页面
3. ⏳ **你需要在浏览器中测试：**
   - 点击"开始对话"按钮
   - 允许麦克风权限
   - 说一句话测试
   - 查看控制台日志和页面状态
   - 确认ASR→AI→TTS流程是否正常

**第二步：对比修复uni-app**
1. 对比HTML和CryptoPhone/api/的代码差异
2. 重点检查：
   - ⭐ AI响应解析（SSE流式格式）- 这是关键问题
   - 录音格式转换
   - 权限请求时机
3. 在CryptoPhone中应用修复

**第三步：HBuilderX真机调试**
1. USB连接手机，真机调试CryptoPhone
2. 测试完整功能流程
3. 查看控制台日志排查问题

**第四步：云打包和部署**
1. 功能验证通过后，云打包APK
2. 手机安装测试
3. 最终目标：手表安装（根据手机测试结果调整）

---

## 📂 项目文件结构

### 核心目录说明
```
watch/
├── CryptoPhone/          ⭐ 主力项目（手机端uni-app）
│   ├── pages/index/index.vue
│   ├── api/              (voice.js, ai.js, crypto.js)
│   ├── utils/config.js   (服务器地址配置)
│   └── keystore/         (Android签名证书)
│
├── CryptoWatch/          📦 备份版本（原始代码）
│
├── server-deploy/        🖥️ 服务器端
│   ├── server.js         (Node.js主服务)
│   └── server-fixed.js   (备份版本)
│
├── web-test-pages/       🧪 HTML测试页面（重要参考）
│   └── voice_chat_complete.html  (完整流程参考)
│
├── 归档文档/             📚 历史文档归档
│   ├── 操作_完整版_20260114.md
│   ├── 操作_备份_20260114.md
│   └── 其他历史文档...
│
└── 文档/
    ├── 项目设计.md       (需求、目标、API密钥)
    ├── 操作.md          (本文件 - 当前进度)
    ├── 文件夹说明.md     (结构详细说明)
    └── 服务器配置代码汇总与解释.md
```

### 📁 关键文件作用说明

**CryptoPhone/pages/index/index.vue**
- 主页面，集成语音对话和加密货币展示
- 包含录音权限申请、录音控制、消息显示逻辑
- 调用voice.js、ai.js、crypto.js封装的API

**CryptoPhone/api/voice.js**
- 阿里云语音API封装（ASR识别、TTS合成）
- 处理音频上传和配置获取
- ⚠️ 需要对比HTML中的正确实现

**CryptoPhone/api/ai.js**
- 扣子AI对话API封装
- SSE流式响应解析
- ⚠️ 响应解析格式需要与HTML对齐

**CryptoPhone/utils/config.js**
- 服务器地址配置：`http://47.97.121.208:8080`
- DEBUG_MODE开关
- 中本聪欢迎语配置

**server-deploy/server.js**
- Node.js后端服务主文件
- 提供所有API代理（ASR/TTS/AI/加密货币）
- ⚠️ 当前版本可能有问题，需要测试

**server-deploy/server-fixed.js**
- 备份版本，可能是之前的稳定版
- 如果server.js有问题，可以回退使用

**web-test-pages/voice_chat_complete.html**
- ⭐ 完整的语音对话实现（已验证可工作）
- 包含正确的API调用、SSE解析、音频处理
- uni-app遇到问题时的重要参考

**项目设计.md**
- 项目需求和最终目标
- 中本聪智能体（次要目标1）、巴菲特智能体（次要目标2）
- **所有API密钥和凭证**（阿里云、扣子、CoinMarketCap、代理）
- 改进代码须知和注意事项

**文件夹说明.md**
- 完整的项目结构说明
- 每个文件夹和关键文件的详细介绍
- 文档维护建议

---

## 🔑 核心配置信息（快速查阅）

### 服务器
```
IP: 47.97.121.208
端口: 8080
SSH: diana@47.97.121.208 (密码: a)
目录: /var/www/CryptoWatchDemo
```

### Android签名证书
```
路径: C:\Users\25841\Desktop\watch\CryptoPhone\keystore\cryptowatch.keystore
密码: cryptowatch2026
别名: cryptowatch
```

### API密钥（详见项目设计.md第4章节）
- **阿里云语音：** AppKey: 0v6AqlfcJJo9dgq8（详见项目设计.md）
- **扣子AI：** Project ID: 7594865794518106153（详见项目设计.md）
- **CoinMarketCap：** API Key见项目设计.md
- **HTTP代理：** 1月16日到期（详见项目设计.md）

> 💡 完整密钥信息请查阅[项目设计.md](项目设计.md)第4章节

---

## 🚀 快速操作命令

### 服务器管理
```bash
# SSH连接
ssh diana@47.97.121.208  # 密码：a

# 查看服务状态
sudo systemctl status cryptowatch

# 重启服务
sudo systemctl restart cryptowatch

# 查看日志
sudo journalctl -u cryptowatch -f
```

### 本地测试
```powershell
# 测试服务器健康
Invoke-WebRequest http://47.97.121.208:8080/

# 测试加密货币API
Invoke-WebRequest http://47.97.121.208:8080/api/crypto/latest

# 启动本地服务器（调试用）
cd C:\Users\25841\Desktop\watch\server-deploy
node server.js
```

### HBuilderX操作
1. 打开项目：`C:\Users\25841\Desktop\watch\CryptoPhone`
2. 运行 → 运行到手机或模拟器 → USB真机调试
3. 查看控制台日志（重点关注 `[Main]`, `[Voice API]`, `[AI API]`）

---

## ⚠️ 重要注意事项

### 代码修改原则（来自项目设计.md）
1. ✅ 所有代码可直接修改（用户会手动备份）
2. ✅ 只维护操作.md一个文档（避免冗余）
3. ⚠️ 关键抉择点需要与用户讨论
4. 🔍 **服务器操作前先检查当前状态，不要盲目修改**
5. 🔍 **注意端口占用和服务运行问题**
6. 🔍 **AI应该静下心来慢慢发现问题，避免急躁**

### Token管理
- **测试Token:** 1a6ce4388c794c7bae76a2f5e2f594ca（24h后失效，1月14日过期）
- **长期方案:** 使用get_aliyun_token.py动态生成
- **AccessKey:** 不能暴露给客户端，只在服务器端使用

### 项目目标
- **最终目标：** 安卓手表智能对话APP（中本聪智能体）
- **当前策略：** 先在手机验证，再适配手表
- **主力项目：** CryptoPhone/（手机端uni-app）
- **次要目标1：** 中本聪智能体 + 加密货币展示
- **次要目标2：** 巴菲特智能体 + A股分析（未开始）

---

## 📝 历史问题记录

### 问题1：为什么之前能运行现在不行？
**分析：**
- 可能之前在Linux/Mac环境测试（用python3命令）
- 或者有缓存的Token文件（未过期）
- 现在Windows环境需要用python而非python3

### 问题2：浏览器显示"localhost拒绝连接"
**原因：** 服务器打印"启动成功"后立即退出
**解决：** 发现是Python调用失败导致配置加载错误

### 问题3：AI急躁问题和重复错误
**反思：**
- AI在看到启动日志就认为成功，没有验证实际运行状态
- 应该先检查端口监听、能否访问，再下结论
- 项目设计.md明确要求：先了解服务器情况再行动

---

## 🔄 重启对话提示

**告诉新AI：**
```
请阅读 项目设计.md 和 操作.md。

当前状态：
1. Python已安装（3.12.1）
2. server.js在8080端口运行，但Python调用失败
3. 需要修改server.js第56行：python3 → python
4. HTML测试页面可访问，但配置加载失败

下一步任务：
1. 修复server.js的python命令
2. 重启服务器
3. 测试HTML页面完整流程（录音→ASR→AI→TTS）
4. 基于HTML成功实现修复CryptoPhone

关键参考：
- web-test-pages/voice_chat_complete.html（正确的实现）
- 特别注意：AI响应的SSE格式解析（操作.md有详细说明）
```

---

**文档版本：** v4.0 完整重启版  
**最后更新：** 2026年1月14日 21:15  
**维护原则：** 详细记录当前状态、文件作用、技术细节
